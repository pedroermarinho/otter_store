import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:otter_store/app/modules/installed/installed_controller.dart';
import 'package:otter_store/app/modules/installed/installed_module.dart';

void main() {
  initModule(InstalledModule());
  // InstalledController installed;
  //
  setUp(() {
    //     installed = InstalledModule.to.get<InstalledController>();
  });

  group('InstalledController Test', () {
    //   test("First Test", () {
    //     expect(installed, isInstanceOf<InstalledController>());
    //   });

    //   test("Set Value", () {
    //     expect(installed.value, equals(0));
    //     installed.increment();
    //     expect(installed.value, equals(1));
    //   });
  });
}
