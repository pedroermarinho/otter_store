import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:otter_store/app/modules/installed/installed_page.dart';

void main() {
  testWidgets('InstalledPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(InstalledPage(title: 'Installed')));
    //  final titleFinder = find.text('Installed');
    //  expect(titleFinder, findsOneWidget);
  });
}
